-- SQL script to create the Fitness-Tracker database
-- Run this in PostgreSQL before starting the Spring Boot application

-- Connect to PostgreSQL as superuser first, then run:
CREATE DATABASE "Fitness-Tracker";

-- Grant privileges (optional, if using a specific user)
-- GRANT ALL PRIVILEGES ON DATABASE "Fitness-Tracker" TO postgres;

-- Verify the database was created
-- \l

